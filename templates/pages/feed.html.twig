{% extends "base.html.twig" %}

{% block title %}
    Feed
{% endblock title %}

{% block stylesheets %}

{% endblock stylesheets %}

{% block body %}
    <h1 class="text-2xl">
        Activity log
    </h1>

    <timeline-wrapper>
        {% for post in posts %}
            <timeline-element type="{{ post.type }}" content="{{ post.preview }}"></timeline-element>
        {% endfor %}
    </timeline-wrapper>
{% endblock body %}

{% block javascripts %}
    {{ encore_entry_script_tags("component:TimelineWrapper") }}
    {{ encore_entry_script_tags("component:TimelineElement") }}
{% endblock javascripts %}
