{% extends "layout.html.twig" %}

{% block title %}
    {{ "legal.conduct.title"|trans }} - {{ parent() }}
{% endblock title %}

{% block stylesheets %}
    {{ encore_entry_link_tags("style:markdown") }}
{% endblock stylesheets %}

{% block body %}
    <section class="markdown-style">
        <h1>
            {{ "legal.conduct.title"|trans }}
        </h1>

        {{ "legal.conduct.text"|trans|raw }}
    </section>
{% endblock body %}
